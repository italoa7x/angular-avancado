import { Component, Input } from '@angular/core';

@Component({
  selector: 'app-area-logado',
  templateUrl: './area-logado.component.html',
  styleUrls: ['./area-logado.component.scss'],
})
export class AreaLogadoComponent {
  //@Input("nome") nome: string = "";
}
